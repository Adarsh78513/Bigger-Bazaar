<!DOCTYPE html>
<html lang="en">

    <%- include('./partials/head.ejs') %>
    <body>
        <% if(orders.length > 0){ %>
            <% orders.forEach( orders => { %>
                    <div class = "product">
                        <!-- <h1 class = "product_name"><%= orders.Product.Name %></h1> -->
                        <p>Address for shipping the product: <%= orders.Address %></p>
                        <p>Purchse points earned: <%= orders.PurchasePoints %></p>
                        <p>Order Date: <%= orders.Orderdate %></p>
                        <p>Order Status: <%= orders.Status %></p>
                        <p>Shipping Date: <%= orders.Shippingdate %></p>
                        <p>Delivery Date: <%= orders.DeliveryDate %></p>
                    </div>
                </a>
                
            <% }) %>
        <% }else { %>
            <p>No orders</p>
        <% } %>
        <%- include('./partials/footer.ejs') %>
    </body>

</html>